import { StyleSheet } from "react-native";

export const authStyles = StyleSheet.create({
  input: {
    minWidth: 250,
    borderWidth: 1,
    borderColor: "#000",
    borderRadius: 20,
    marginBottom: 16,
    padding: 16,
    backgroundColor: "#fff",
    color: "#111",
  },
});
